# Additional dependencies for the fork's API server (server/tts_server_async.py).
# The core TTS dependencies come from the qwen-tts package itself (see pyproject.toml).
#
# Install with:  pip install -r server/requirements-server.txt
#
# Note: Uses eager (standard PyTorch) attention.
# RTX 5090 (32 GB VRAM) easily supports eager attention with all models loaded (~12-14 GB).
# Eager is 100% officially supported, with zero security or maintenance risk.
#
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.31
